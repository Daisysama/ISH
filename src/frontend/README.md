# src/frontend / 前端 UI

这里放可复用的界面组件与浏览器交互。

当前目录：

- `components/auth/`：登录 / 注册表单；
- `components/brand/`：品牌组件；
- `components/projects/`：“咩”表单、项目状态展示；
- `components/moderation/`：管理员审核交互。

Client Component 只处理用户交互；权限、数据库写入和最终业务判断必须由服务端完成。
